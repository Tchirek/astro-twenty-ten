---
interface Props {
  summary: string;
  open?: boolean;
}

const { summary, open = false } = Astro.props;
---

<details class="mdx-details" open={open}>
  <summary>{summary}</summary>
  <div class="mdx-details-content"><slot /></div>
</details>
